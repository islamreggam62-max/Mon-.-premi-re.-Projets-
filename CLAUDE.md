# تعليمات المشروع

## فريق وكلاء Shopify

يحتوي `.claude/agents/` على ثلاثة وكلاء يعملون معاً على متجر Shopify
(التفاصيل في `docs/shopify-agents.md`):

1. **shopify-analyst** — قراءة فقط؛ يحلّل ويُخرج «تقرير التسليم» بتوصيات `[C#]` و`[M#]`.
2. **shopify-catalog** — ينفّذ توصيات `[C#]` (منتجات، مجموعات، مخزون).
3. **shopify-marketing** — ينفّذ توصيات `[M#]` (خصومات، حملات).

عند طلب مهمّة شاملة على المتجر (مثل «حسّن متجري» أو «جهّز حملة»)، نسّق الفريق:

1. شغّل `shopify-analyst` أولاً.
2. اعرض تقرير التسليم على المستخدم واطلب موافقته على التوصيات.
3. شغّل `shopify-catalog` مع توصيات `[C#]` الموافق عليها (انسخ التقرير كاملاً في الطلب).
4. شغّل `shopify-marketing` مع توصيات `[M#]` الموافق عليها + سجلّ الكتالوج.
5. لخّص للمستخدم ما تغيّر فعليًّا، وما يجب مراقبته.

الوكلاء لا يتواصلون مباشرة؛ ينقل الوكيل الرئيسي التقارير بينهم حرفيًّا.
للمهام البسيطة والمباشرة يمكن استعمال الوكيل العام `shopify`.
